<div class="container-fluid bg-dark-gray-blue text-white py-3 sticky-toolbar">
  <div class="d-flex align-items-center justify-content-between default-padding">
    <div class="d-flex align-items-center gap-3">
      <% if local_assigns[:back_path] %>
        <%= link_to "← Back", back_path, class: "btn btn-ghost" %>
      <% end %>
      <h1 class="mb-0 font-primary-title"><%= title %></h1>
    </div>
    <div class="d-flex align-items-center gap-2">
      <%= yield if block_given? %>
    </div>
  </div>
</div>
<div class="container-fluid bg-gold py-1"></div>

