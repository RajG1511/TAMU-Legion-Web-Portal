<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Legion" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta http-equiv="Cache-Control" content="no-store, no-cache, must-revalidate, max-age=0">

    <%= yield :head %>
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Includes all stylesheet files in app/assets/stylesheets -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", crossorigin: "anonymous" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload"%>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <link rel="stylesheet" href="/manual.css" media="all">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
  </head>

  <body class="d-flex flex-column min-vh-100">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-white px-4 mt-4">
      <div class="container-fluid border-bottom-dark-navy mb-2">
        <!-- Logo on the left -->
        <%= link_to root_path, class: "d-flex align-items-center me-4" do %>
          <img src="/Logos/legion large blue.png" alt="TAMU Legion Logo" class="navbar-logo" style="height:50px;">
        <% end %>

        <!-- Hamburger button on the right for mobile view -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible navbar links -->
        <div class="collapse navbar-collapse" id="mainNavbar">
          <!-- Navbar links -->
          <ul class="navbar-nav font-primary-heading ms-auto">
            <% nav_links = {
              "Home" => root_path,
              "About Us" => about_path,
              "Recruitment" => recruitment_path,
              "Members" => members_path,
              "Committees" => committees_path,
              "Contact Us" => contact_path,
              "Member Center" => member_center_path
            } %>

            <% nav_links.each do |name, path| %>
              <% if name == "Committees" %>
                <!-- Committees dropdown -->
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle text-dark-navy" href="#" id="committeesDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" data-turbo="false">
                    Committees
                  </a>
                  <ul class="dropdown-menu dropdown-menu-legion" aria-labelledby="committeesDropdown">
                    <% if @nav_committees.present? %>
                      <% @nav_committees.each do |committee| %>
                        <li><%= link_to committee.name, committee_path(committee), class: "dropdown-item dropdown-item-legion" %></li>
                      <% end %>
                      <li><hr class="dropdown-divider dropdown-divider-legion"></li>
                      <li><%= link_to "All Committees", committees_path, class: "dropdown-item dropdown-item-legion" %></li>
                    <% else %>
                      <li><span class="dropdown-item-text text-muted">No Committees</span></li>
                    <% end %>
                  </ul>
                </li>
              <% else %>
                <% active_class = current_page?(path) ? "active-link" : "" %>
                <li class="nav-item">
                  <%= link_to name, path, class: "nav-link text-dark-navy #{active_class}" %>
                </li>
              <% end %>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% flash.each do |key, message| %>
        <div class="container-fluid bg-gold text-dark-navy py-0">
            <div class="alert alert-dismissible fade show mb-0 text-center" role="alert" style="background-color: transparent; border: none;">
                <strong><%= message %></strong>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% end %>

    <main class="flex-grow-1">
      <%= yield %>
    </main>


    <!-- Footer -->
    <footer class="footer bg-dark-navy text-white py-3 mt-auto font-secondary-body">
      <div class="container d-flex justify-content-between align-items-center flex-wrap">
        <!-- Left side: copyright & contact -->
        <div class="footer-left">
          &copy; <%= Time.current.year %> by LEGION Men's Organization |
          <%= link_to "Contact Us", contact_path, class: "text-gold text-decoration-none" %> | 
          <%= link_to "Contact Support", "mailto:kylepalermo@tamu.edu", class: "text-gold text-decoration-none"%>
        </div>

        <!-- Right side: social icons -->
        <div class="footer-right d-flex gap-2">
          <a href="https://www.instagram.com/tamulegion/" target="_blank" class="footer-icon d-flex align-items-center justify-content-center">
            <i class="bi bi-instagram"></i>
          </a>
          <a href="https://www.linkedin.com/company/tamulegion/mycompany/?viewAsMember=true" target="_blank" class="footer-icon d-flex align-items-center justify-content-center">
            <i class="bi bi-linkedin"></i>
          </a>
        </div>
      </div>
    </footer>

 
  </body>
</html>

