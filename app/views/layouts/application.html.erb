<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Legion" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", crossorigin: "anonymous" %>
  </head>

  <body class="bg-light d-flex flex-column min-vh-100">
    <!-- Top Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Legion</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item"><%= link_to "Events Dashboard", dashboard_events_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Resources Dashboard", dashboard_resources_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Events", events_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Resources", resources_path, class: "nav-link" %></li>
          </ul>
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link" %></li>
            <% else %>
              <li class="nav-item"><%= link_to "Sign in with Google", user_google_oauth2_omniauth_authorize_path, class: "nav-link" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <div class="container">
      <% flash.each do |key, message| %>
        <% bootstrap_class = case key.to_sym
          when :notice then "info"
          when :success then "success"
          when :error then "danger"
          when :alert then "warning"
          else "secondary"
        end %>
        <div class="alert alert-<%= bootstrap_class %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <!-- Secondary nav -->
    <header class="bg-white border-bottom mb-3">
      <div class="container py-2">
        <nav class="nav">
          <%= link_to "Home", root_path, class: "nav-link" %>
          <%= link_to "Members", users_path, class: "nav-link" %>

          <!-- Service Hours Tabs (visible to everyone for now) -->
          <%= link_to "Submit Service Hours", new_service_path, class: "nav-link" %>
          <%= link_to "My Service Requests", services_path(user_id: current_user&.id), class: "nav-link" %>
          <%= link_to "Approve/Deny Service Hours", services_path, class: "nav-link" %>
        </nav>
      </div>
    </header>

    <!-- Main content -->
    <main class="container flex-grow-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white text-center py-3 mt-auto">
      <p class="mb-0">&copy; <%= Time.current.year %> Legion</p>
    </footer>
  </body>
</html>