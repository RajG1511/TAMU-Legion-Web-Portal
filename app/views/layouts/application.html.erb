<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Legion" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", crossorigin: "anonymous" %>
  </head>

  <body>
    <nav>
      <%= link_to "Events Dashboard", dashboard_events_path %> |
      <% if user_signed_in? && (current_user.exec? || current_user.president?) %>
        <%= link_to "Resources Dashboard", dashboard_resources_path %> |
      <% end %>
      <%= link_to "Events", events_path %> |
      <%= link_to "Resources", resources_path %>

      <% if user_signed_in? && (current_user.exec? || current_user.president?) %>
        | <%= link_to "Manage Users", users_path %>
      <% end %>

      <!-- Service Hours navigation (visible to everyone) -->
      | <%= link_to "Submit Service Hours", new_service_path %>
      | <%= link_to "View Service Hours", services_path %>
      | <%= link_to "Approve/Reject Service Hours", services_path %>
    </nav>

    <p style="color: red"><%= flash[:error] %></p>
    <p style="color: blue"><%= flash[:notice] %></p>
    <p style="color: green"><%= flash[:success] %></p>

    <% if user_signed_in? %>
      <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { turbo: false } %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path, data: { turbo: false } %>
    <% end %>

    <%= yield %>

    <% flash.each do |key, message| %>
      <div class="flash <%= key %>"><%= message %></div>
    <% end %>
  </body>
</html>