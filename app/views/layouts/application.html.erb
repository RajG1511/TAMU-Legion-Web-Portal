<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Legion" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Stylesheets -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <link rel="stylesheet" href="/manual.css" media="all">
  </head>

  <body style="font-family: 'Public Sans', sans-serif;">
    <nav style="background-color: #0d1b3d; color: white; padding: 10px; font-weight: 500;">
      <%= link_to "Home", root_path, style: "color: white; text-decoration: none; margin-right: 10px;" %> |
      <%= link_to "Events", events_path, style: "color: white; text-decoration: none; margin-right: 10px;" %> |
      <%= link_to "Resources", resources_path, style: "color: white; text-decoration: none; margin-right: 10px;" %> |
      <%= link_to "Submit Service Hours", new_service_path, style: "color: white; text-decoration: none; margin-right: 10px;" %> |
      <%= link_to "View Service Hours", services_path, style: "color: white; text-decoration: none; margin-right: 10px;" %> |
      <%= link_to "Approve/Reject Service Hours", services_path, style: "color: white; text-decoration: none; margin-right: 10px;" %>

      <% if user_signed_in? && (current_user.exec? || current_user.president?) %>
        | <%= link_to "Events Dashboard", dashboard_events_path, style: "color: #FFD700; text-decoration: none; margin-right: 10px;" %>
        | <%= link_to "Resources Dashboard", dashboard_resources_path, style: "color: #FFD700; text-decoration: none; margin-right: 10px;" %>
        | <%= link_to "Manage Users", users_path, style: "color: #FFD700; text-decoration: none; margin-right: 10px;" %>
        | <%= link_to "Edit Home", edit_home_path, style: "color: #FFD700; text-decoration: none; margin-right: 10px;" %>
      <% end %>

      <% if user_signed_in? %>
        <span style="float: right;">
          <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { turbo: false }, style: "color: #ff6666; text-decoration: none;" %>
        </span>
      <% else %>
        <span style="float: right;">
          <%= link_to "Sign in", new_user_session_path, data: { turbo: false }, style: "color: #FFD700; text-decoration: none;" %>
        </span>
      <% end %>
    </nav>

    <div style="margin: 20px;">
      <p style="color: red;"><%= flash[:error] %></p>
      <p style="color: blue;"><%= flash[:notice] %></p>
      <p style="color: green;"><%= flash[:success] %></p>

      <%= yield %>

      <% flash.each do |key, message| %>
        <div class="flash <%= key %>"><%= message %></div>
      <% end %>
    </div>
  </body>
</html>

