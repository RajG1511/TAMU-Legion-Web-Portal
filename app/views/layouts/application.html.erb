<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Legion" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <link href="https://fonts.googleapis.com/css2?family=Public+Sans:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Includes all stylesheet files in app/assets/stylesheets -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", crossorigin: "anonymous" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <link rel="stylesheet" href="/manual.css" media="all">
  </head>

  <body class="bg-white text-legion-blue">
    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark-navy sticky-top">
      <div class="container">
        <%= link_to "Legion", root_path, class: "navbar-brand fw-bold" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
          <!-- Left side -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Home", root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Events", events_path, class: "nav-link #{'active' if controller_name=='events'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Resources", resources_path, class: "nav-link #{'active' if controller_name=='resources'}" %>
            </li>
            <li class="nav-item">
              <%= link_to "Committees", committees_path, class: "nav-link #{'active' if controller_name=='committees'}" %>
            </li>

            <!-- Service Hours dropdown (visible to everyone) -->
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="servicesMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Service Hours
              </a>
              <ul class="dropdown-menu" aria-labelledby="servicesMenu">
                <li><%= link_to "Submit Service Hours", new_service_path, class: "dropdown-item" %></li>
                <li><%= link_to "View Service Hours", services_path, class: "dropdown-item" %></li>
                <li><%= link_to "Approve/Reject Service Hours", services_path, class: "dropdown-item" %></li>
              </ul>
            </li>

            <% if user_signed_in? && (current_user.exec? || current_user.president?) %>
              <!-- Exec/President-only quick links -->
              <li class="nav-item">
                <%= link_to "Events Dashboard", dashboard_events_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Resources Dashboard", dashboard_resources_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Manage Users", users_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>

          <!-- Right side -->
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? && (current_user.exec? || current_user.president?) %>
              <li class="nav-item me-2">
                <%= link_to "Edit Home", edit_home_path, class: "btn btn-outline-light btn-sm" %>
              </li>
            <% end %>

            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="userMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <%= current_user.full_name %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenu">
                  <li><%= link_to "Profile", user_path(current_user), class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <!-- Your routes define GET /users/sign_out to Devise#destroy -->
                  <li><%= link_to "Sign out", destroy_user_session_path, class: "dropdown-item", data: { turbo: false } %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign in", new_user_session_path, class: "btn btn-gold btn-sm", data: { turbo: false } %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- FLASHES -->
    <div class="container mt-3">
      <% flash.each do |key, message| %>
        <%# map rails keys to bootstrap classes %>
        <% css = { notice: "alert-info", success: "alert-success", error: "alert-danger", alert: "alert-warning" }[key.to_sym] || "alert-info" %>
        <div class="alert <%= css %> alert-dismissible fade show default-border-radius" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>

