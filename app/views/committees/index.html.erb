<h1>Committees</h1>

<h2>Select a committee for more information</h2>

<ul id="committee-list">
    <% @committees.each do |committee| %>
        <li>
            <%= link_to committee.name, committee_path(committee) %>
        </li>
    <% end %>
</ul>
