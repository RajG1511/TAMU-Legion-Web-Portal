<div class="container-fluid bg-dark-gray-blue text-white py-3">
  <h2 class="text-center font-primary-title">Delete Member</h2>
</div>
<div class="container-fluid bg-gold py-1"></div>

<div class="container my-4 p-4 border border-4 border-white bg-dark-navy text-white default-border-radius">
  <p>Are you sure you want to delete <strong><%= @user.full_name %></strong>? This action cannot be undone.</p>
  <div class="d-flex gap-2 mt-3">
    <%= button_to "Confirm Delete", user_path(@user), method: :delete, class: "btn btn-gold text-dark-navy" %>
    <%= link_to "Back", users_path, class: "btn btn-gold text-dark-navy" %>
  </div>
  <p class="mt-3 text-warning">Note: If this user has audit history, deletion is blocked to preserve logs.</p>
</div>
<div class="container-fluid bg-gold py-1"></div>

