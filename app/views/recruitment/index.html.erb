<% content_for :title, "Recruitment" %>

<section class="hero bg-dark-navy text-white py-5">
  <div class="container">
    <div class="hero__badge-inner">
      <%= raw(@sections["hero_badge_html"]) %>
    </div>
    <div class="mt-2">
      <%= raw(@sections["hero_subtitle_html"]) %>
    </div>
    <div class="mt-3">
      <%= raw(@sections["cta_buttons_html"]) %>
    </div>
  </div>
</section>

<section class="py-5 bg-legion-blue text-white">
  <div class="container">
    <div class="row g-4 align-items-start">
      <div class="col-md-6">
        <%= raw(@sections["body_lead_html"]) %>
      </div>
      <div class="col-md-6 text-center">
        <!-- your calendar flyer -->
        <%= image_tag "/Flyers/Calendar_FA25.png",
                      alt: "Recruitment Calendar",
                      class: "img-fluid border border-4 border-white" %>
      </div>
    </div>
  </div>
</section>

<section class="py-5 bg-dark-navy text-white">
  <div class="container">
    <%= raw(@sections["contact_html"]) %>
  </div>
</section>

